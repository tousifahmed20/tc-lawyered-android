package dev.tclawyered.app

import android.app.Application

/**
 * Application entry point. A holder for process-wide singletons (hive client,
 * settings repository) as they come online in later slices.
 */
class TcLawyeredApp : Application()
